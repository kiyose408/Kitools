好的，根据之前的讨论和任务分解，为您生成Markdown格式的文档。

---

## PC效率工具箱 - 开发说明

**项目名称**: PC效率工具箱  
**项目负责人**: [项目经理姓名]  
**当前状态**: 立项与需求分析完成  
**目标平台**: Windows  
**开发框架**: Qt (C++)  
**主要功能模块**: 桌面置顶计时器、桌面待办事项  

---

### 总体开发任务分解

我们将开发任务按功能模块进行划分，并进一步细分为可执行的子任务。

---

### 模块一：桌面置顶计时器 (Desktop Overlay Timer)

**ID**: M1  
**优先级**: P0 (最高)  
**描述**: 实现一个可置顶于桌面的倒计时器，支持单次和循环（番茄钟）模式，并提供丰富的自定义外观选项和提示功能。

| 任务ID    | 任务名称             | 描述                                                         | 依赖         | 状态  | 负责人 |
| :-------- | :------------------- | :----------------------------------------------------------- | :----------- | :---- | :----- |
| **M1-T1** | **基础UI与计时逻辑** |                                                              |              |       |        |
| M1-T1.1   | 设计主窗口UI         | 使用Qt Designer或手写代码创建主窗口，包含时分秒输入框、开始按钮、模式切换选项卡。 | -            | To Do |        |
| M1-T1.2   | 实现单次计时逻辑     | 使用`QTimer`实现基础的倒计时功能，更新UI显示。               | M1-T1.1      | To Do |        |
| M1-T1.3   | 实现循环计时逻辑     | 在`M1-T1.2`基础上，增加工作/休息阶段切换、循环次数计数逻辑。 | M1-T1.2      | To Do |        |
| M1-T1.4   | 添加模式切换功能     | 实现单次/循环模式的切换，UI随之动态变化（显示/隐藏循环设置项）。 | M1-T1.1      | To Do |        |
| **M1-T2** | **置顶与显示窗口**   |                                                              |              |       |        |
| M1-T2.1   | 设计置顶窗口UI       | 创建一个轻量级的`QWidget`，用于显示倒计时数字，无边框。      | -            | To Do |        |
| M1-T2.2   | 实现置顶功能         | 设置窗口标志`Qt::WindowStaysOnTopHint`，确保窗口始终在最前。 | M1-T2.1      | To Do |        |
| M1-T2.3   | 实现窗口拖动         | 重写`mousePressEvent`和`mouseMoveEvent`，使置顶窗口可拖动。  | M1-T2.1      | To Do |        |
| **M1-T3** | **外观与自定义**     |                                                              |              |       |        |
| M1-T3.1   | 添加自定义设置UI     | 在主窗口增加面板，用于设置字体大小、背景色、透明度滑块。     | M1-T1.1      | To Do |        |
| M1-T3.2   | 实现透明度调节       | 通过`setStyleSheet`或`setAttribute(Qt::WA_TranslucentBackground)`和`QPalette`实现数字和背景的透明度调节。 | M1-T2.1, M1-T3.1 | To Do |        |
| M1-T3.3   | 实现背景颜色/图片自定义 | 允许用户选择背景色或加载背景图片，并应用到置顶窗口。         | M1-T2.1, M1-T3.1 | To Do |        |
| M1-T3.4   | 实现尺寸调节         | 允许用户通过主窗口的输入或滑块调整置顶窗口的宽高。           | M1-T2.1, M1-T3.1 | To Do |        |
| **M1-T4** | **提示与状态**       |                                                              |              |       |        |
| M1-T4.1   | 实现视觉提示（闪烁） | 计时结束时，让置顶窗口或数字部分进行闪烁。                   | M1-T2.1      | To Do |        |
| M1-T4.2   | 实现听觉提示（蜂鸣） | 计时结束时，播放系统蜂鸣或自定义音效。                       | M1-T2.1      | To Do |        |
| M1-T4.3   | 实现状态显示（工作/休息） | 在置顶窗口上显示当前是“工作”还是“休息”，并可选不同颜色。     | M1-T1.3, M1-T2.1 | To Do |        |
| **M1-T5** | **数据持久化**       |                                                              |              |       |        |
| M1-T5.1   | 添加设置管理         | 使用`QSettings`保存用户常用的计时设置（默认时长、透明度等）。 | -            | To Do |        |
| **M1-T6** | **集成与测试**       |                                                              |              |       |        |
| M1-T6.1   | 集成控制器           | 创建`PomodoroTimerManager`，连接主窗口、置顶窗口和设置管理器。 | M1-T1.\*, M1-T2.\*, M1-T3.\*, M1-T4.\*, M1-T5.1 | To Do |        |
| M1-T6.2   | 功能测试             | 对所有功能进行全面测试，修复bug。                            | M1-T6.1      | To Do |        |

---

### 模块二：桌面待办事项 (Desktop To-Do List)

**ID**: M2  
**优先级**: P1 (高)  
**描述**: 实现一个可固定在桌面的待办列表，支持任务增删改查、完成标记、进度评分，并通过系统托盘进行管理，同时记录详细日志并支持导入导出。

| 任务ID    | 任务名称             | 描述                                                         | 依赖         | 状态  | 负责人 |
| :-------- | :------------------- | :----------------------------------------------------------- | :----------- | :---- | :----- |
| **M2-T1** | **数据模型与管理**   |                                                              |              |       |        |
| M2-T1.1   | 定义任务数据结构     | 创建`struct TaskData`，包含ID, Description, IsCompleted, CompletionScore, Timestamps等字段。 | -            | To Do |        |
| M2-T1.2   | 定义日志数据结构     | 创建`struct LogEntry`，包含Timestamp, Action, TaskDescription, Score等字段。 | -            | To Do |        |
| M2-T1.3   | 实现任务管理器       | 创建`TaskManager`类，实现任务的CRUD、标记完成、评分等核心逻辑。 | M2-T1.1      | To Do |        |
| M2-T1.4   | 实现日志记录器       | 创建`LogManager`类，实现日志的记录、存储（内存/文件）、导出(CSV/JSON)、导入功能。 | M2-T1.2      | To Do |        |
| **M2-T2** | **基础UI与交互**     |                                                              |              |       |        |
| M2-T2.1   | 设计工具箱初始化UI   | 在工具箱主界面为待办事项模块添加初始化入口，包含添加任务的输入框和按钮。 | -            | To Do |        |
| M2-T2.2   | 设计桌面待办窗口UI   | 创建`DesktopToDoWidget`，包含一个`QVBoxLayout`用于存放任务项。 | -            | To Do |        |
| M2-T2.3   | 设计单个任务项UI     | 创建一个自定义`QWidget`作为`TaskItemWidget`，包含`QCheckBox`, `QLabel`, `QSlider`。 | M2-T2.2      | To Do |        |
| M2-T2.4   | 实现任务项交互       | 实现`TaskItemWidget`中复选框和滑动条的信号连接，通知`TaskManager`更新状态和分数。 | M2-T2.3, M2-T1.3 | To Do |        |
| **M2-T3** | **显示与模式控制**   |                                                              |              |       |        |
| M2-T3.1   | 实现置顶模式         | 为`DesktopToDoWidget`设置`Qt::WindowStaysOnTopHint`标志。    | M2-T2.2      | To Do |        |
| M2-T3.2   | 实现桌面固定模式     | 探索并实现`DesktopToDoWidget`固定在桌面区域（可能使用`Qt::Tool`等组合标志）。 | M2-T2.2      | To Do |        |
| M2-T3.3   | 实现窗口拖动         | 为`DesktopToDoWidget`添加拖动功能。                          | M2-T2.2      | To Do |        |
| **M2-T4** | **系统托盘集成**     |                                                              |              |       |        |
| M2-T4.1   | 创建系统托盘图标     | 使用`QSystemTrayIcon`创建图标。                              | -            | To Do |        |
| M2-T4.2   | 创建系统托盘菜单     | 实现包含“显示/隐藏”、“切换模式”、“退出”的右键菜单。         | M2-T4.1      | To Do |        |
| M2-T4.3   | 连接菜单功能         | 实现菜单项点击后，控制`DesktopToDoWidget`的显示/隐藏和模式切换。 | M2-T4.2, M2-T3.\* | To Do |        |
| **M2-T5** | **数据持久化与日志** |                                                              |              |       |        |
| M2-T5.1   | 集成日志记录         | 在`TaskManager`中，每当任务状态改变时，调用`LogManager`记录日志。 | M2-T1.4, M2-T1.3 | To Do |        |
| M2-T5.2   | 实现日志导出/导入UI  | 在工具箱主界面或系统托盘菜单中添加导出/导入按钮。            | M2-T1.4      | To Do |        |
| M2-T5.3   | 实现日志导出/导入逻辑 | 连接UI按钮到`LogManager`的`export`/`import`方法。            | M2-T5.1, M2-T5.2 | To Do |        |
| **M2-T6** | **集成与测试**       |                                                              |              |       |        |
| M2-T6.1   | 集成控制器           | 创建一个控制器类，连接`TaskManager`, `LogManager`, `DesktopToDoWidget`, `SystemTrayController`。 | M2-T1.\*, M2-T2.\*, M2-T3.\*, M2-T4.\*, M2-T5.\* | To Do |        |
| M2-T6.2   | 功能测试             | 对所有功能进行全面测试，修复bug。                            | M2-T6.1      | To Do |        |

---

### 通用/基础设施任务

**ID**: INFRA  
**描述**: 项目整体的基础设施搭建。

| 任务ID  | 任务名称       | 描述                                           | 依赖      | 状态  | 负责人 |
| :------ | :------------- | :--------------------------------------------- | :-------- | :---- | :----- |
| INFRA-T1 | 项目初始化     | 创建Qt项目，配置`.pro`文件，设置基本目录结构。 | -         | To Do |        |
| INFRA-T2 | 工具箱主框架   | 创建一个主窗口作为工具箱容器，可以加载和切换不同的工具模块（M1, M2）。 | INFRA-T1  | To Do |        |

---

### 仓库结构建议

为了方便管理，建议的Git仓库结构如下：

```
PC-Productivity-Toolbox/
├── src/                    # 源代码根目录
│   ├── core/               # 核心框架代码 (如工具箱主框架)
│   ├── modules/            # 各个工具模块的代码
│   │   ├── timer/          # 桌面置顶计时器模块代码
│   │   │   ├── ui/
│   │   │   ├── logic/
│   │   │   └── ...
│   │   └── todo/           # 桌面待办事项模块代码
│   │       ├── ui/
│   │       ├── logic/
│   │       └── ...
│   └── main.cpp
├── docs/                   # 文档 (如本开发说明、SRS、Arch Doc)
├── assets/                 # 资源文件 (图标、音效等)
├── tests/                  # 测试代码 (可选)
└── .gitignore
```

---

## 软件需求说明书 (SRS) - PC效率工具箱

**文档版本**: 1.0  
**日期**: 2026-02-19  
**项目**: PC效率工具箱  

### 1. 引言

#### 1.1 目的
本文档旨在定义“PC效率工具箱”软件的需求，明确其功能、性能、接口和约束，为开发、测试和验收提供依据。

#### 1.2 项目范围
本项目开发一个名为“PC效率工具箱”的Windows桌面应用程序，主要面向长时间使用PC的用户，提供一系列提升效率和改善使用体验的工具。

#### 1.3 定义、缩写词和缩略语
*   SRS: Software Requirements Specification (软件需求说明书)
*   PC: Personal Computer (个人计算机)

#### 1.4 参考文献
*   [无]

#### 1.5 文档概述
本文档第二部分描述了产品的整体概览。第三部分详细阐述了各功能模块的具体需求。第四部分描述了非功能性需求。

### 2. 产品概述

#### 2.1 产品愿景
为长时间使用PC的用户打造一个集成化的效率工具平台，通过一系列小巧、实用、可定制的工具，帮助用户管理工作时间、提高专注力、管理任务，从而提升整体工作和生活效率。

#### 2.2 产品功能摘要
*   **桌面置顶计时器**: 支持单次/循环（番茄钟）计时，可置顶显示，支持自定义外观和提示。
*   **桌面待办事项**: 可固定在桌面的待办列表，支持任务管理、状态跟踪、评分和日志。

#### 2.3 用户特征
*   **主要用户**: 长时间使用PC的专业人士、开发者、设计师、内容创作者、办公人员。

#### 2.4 运行环境
*   **操作系统**: Microsoft Windows 10, Windows 11

#### 2.5 设计和实现约束
*   **开发语言**: C++
*   **开发框架**: Qt
*   **部署方式**: 独立可执行文件 (.exe)

#### 2.6 假设和依赖
*   用户的PC已安装兼容的Windows操作系统。
*   用户具有基本的Windows软件操作能力。

### 3. 系统特性

#### 3.1 桌面置顶计时器 (Desktop Overlay Timer)

##### 3.1.1 功能概述
提供一个可在桌面顶层持续显示的倒计时器，支持单次和循环计时模式，适用于番茄工作法、任务计时等场景。

##### 3.1.2 详细功能需求 (FR)

*   **FR-001: 单次计时设置**
    *   **描述**: 用户应能通过界面输入或选择一个倒计时的总时长（时、分、秒）。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-002: 循环计时设置**
    *   **描述**: 用户应能选择循环计时模式，并分别设置工作时长、休息时长和总循环次数。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-003: 计时执行**
    *   **描述**: 用户点击“开始”后，计时器应立即以倒计时方式更新显示。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-004: 模式切换**
    *   **描述**: 用户应能在单次和循环模式之间切换。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-005: 置顶显示**
    *   **描述**: 计时器窗口应能开启或关闭置顶功能，开启后始终位于所有窗口之上。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-006: 窗口拖动**
    *   **描述**: 计时器窗口应支持鼠标拖动，以改变其在屏幕上的位置。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-007: 尺寸调节**
    *   **描述**: 用户应能调整计时器窗口的显示尺寸。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-008: 数字透明度调节**
    *   **描述**: 用户应能调节计时器数字部分的透明度。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-009: 背景自定义**
    *   **描述**: 用户应能设置计时器窗口的背景颜色或背景图片。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-010: 背景透明度调节**
    *   **描述**: 用户应能调节计时器窗口背景的整体透明度。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-011: 状态区分 (循环模式)**
    *   **描述**: (循环模式下) 计时器应能根据当前是“工作”还是“休息”阶段，通过颜色或文字等方式进行视觉区分。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-012: 视觉提示**
    *   **描述**: 计时结束后，计时器应至少提供一种视觉提示（如闪烁）。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-013: 听觉提示**
    *   **描述**: 计时结束后，计时器应至少提供一种听觉提示（如蜂鸣）。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-014: 设置持久化**
    *   **描述**: 用户的常用设置（如默认时长、透明度）应被保存，在下次启动时恢复。
    *   **优先级**: 低
    *   **来源**: 用户需求

#### 3.2 桌面待办事项 (Desktop To-Do List)

##### 3.2.1 功能概述
提供一个可固定在桌面的轻量化待办列表，方便用户随时查看和管理任务进度。

##### 3.2.2 详细功能需求 (FR)

*   **FR-001: 初始化待办列表**
    *   **描述**: 用户应在工具箱主界面中，能够创建一个新的待办列表实例。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-002: 添加待办事项**
    *   **描述**: 用户应能向待办列表中添加新的任务项，包含描述文本。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-003: 编辑待办事项**
    *   **描述**: 用户应能修改已存在任务项的描述文本。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-004: 完成标记**
    *   **描述**: 每个任务项应包含一个复选框，用户勾选后表示该任务已完成。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-005: 完成度评分**
    *   **描述**: 每个任务项应包含一个滑动条，用户可通过拖动为任务的完成度打分（如0%-100%）。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-006: 窗口显示模式**
    *   **描述**: 待办列表窗口应提供“置顶模式”和“桌面固定模式”两种显示选项。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-007: 窗口拖动**
    *   **描述**: 待办列表窗口应支持鼠标拖动，以改变其在屏幕上的位置。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-008: 系统托盘管理**
    *   **描述**: 应用应创建系统托盘图标，通过右键菜单可实现“显示/隐藏”窗口和“切换显示模式”。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-009: 日志记录**
    *   **描述**: 系统应自动记录任务的创建、完成、评分行为的时间戳、任务描述和评分值。
    *   **优先级**: 高
    *   **来源**: 用户需求

*   **FR-010: 日志导出**
    *   **描述**: 用户应能将记录的日志导出为结构化文件（如JSON或CSV）。
    *   **优先级**: 中
    *   **来源**: 用户需求

*   **FR-011: 日志导入**
    *   **描述**: 用户应能从本地文件导入日志，追加或覆盖当前日志。
    *   **优先级**: 中
    *   **来源**: 用户需求

### 4. 非功能性需求 (NFR)

#### 4.1 性能需求
*   **NFR-001: 计时精度**: 计时器应保持精确的时间计算，误差小于1秒/小时。
*   **NFR-002: UI响应**: 应用程序的UI响应应流畅，不应因列表项增多或设置调整而明显卡顿。

#### 4.2 可靠性需求
*   **NFR-003: 稳定性**: 应用程序应稳定运行，不应因正常使用而崩溃。
*   **NFR-004: 数据完整性**: 日志记录过程不应丢失数据。

#### 4.3 可用性需求
*   **NFR-005: 易用性**: UI应简洁直观，核心功能易于访问。
*   **NFR-006: 误触防护**: 通过系统托盘管理窗口状态，减少误操作。

#### 4.4 兼容性需求
*   **NFR-007: 系统兼容性**: 应在Windows 10和Windows 11上正常运行。

---

## 架构设计文档 (ADD) - PC效率工具箱

**文档版本**: 1.0  
**日期**: 2026-02-19  
**项目**: PC效率工具箱  

### 1. 引言

#### 1.1 目的
本文档旨在定义“PC效率工具箱”的软件架构，描述系统的整体结构、核心组件及其交互关系，为开发团队提供统一的实现蓝图。

#### 1.2 范围
本文档涵盖PC效率工具箱的整体架构，以及其核心模块（桌面置顶计时器、桌面待办事项）的详细设计。

#### 1.3 定义、缩写词和缩略语
*   ADD: Architecture Design Document (架构设计文档)
*   UI: User Interface (用户界面)
*   API: Application Programming Interface (应用程序编程接口)

#### 1.4 参考文献
*   软件需求说明书 (SRS) - PC效率工具箱

#### 1.5 概述
本文档首先介绍系统的总体架构风格，然后分别详述两个核心功能模块的架构设计。

### 2. 总体架构

#### 2.1 架构风格
本系统采用 **插件化模块架构**。核心是一个**工具箱主框架**，负责加载和管理各个独立的**工具模块**。这种设计使得功能模块间耦合度低，易于扩展和维护。

#### 2.2 技术栈
*   **开发语言**: C++
*   **核心框架**: Qt 6.x / 5.x
*   **UI框架**: Qt Widgets
*   **系统交互**: Qt Core, Qt GUI, Windows API (用于置顶、系统托盘等)

#### 2.3 核心组件

*   **主框架 (Main Framework)**:
    *   **职责**: 提供应用的主窗口/主进程入口，作为工具箱的容器，负责加载、卸载和切换工具模块。
    *   **实现**: 一个继承自`QMainWindow`或`QWidget`的类，内部可能使用`QStackedWidget`来切换不同模块的UI。

*   **工具模块 (Tool Modules)**:
    *   **职责**: 实现具体的工具功能（如计时器、待办事项）。
    *   **实现**: 每个模块都是一个独立的库或一组紧密关联的类，遵循主框架定义的接口规范，可以被主框架加载和调用。

*   **通用服务 (Common Services)**:
    *   **职责**: 提供各模块可能用到的通用服务，如日志记录、配置管理等。
    *   **实现**: 可能是一个静态库或单例类集合。

### 3. 模块一：桌面置顶计时器 (Desktop Overlay Timer)

#### 3.1 模块概述
实现一个可置顶于桌面的倒计时器，支持单次和循环（番茄钟）模式。

#### 3.2 模块架构图 (文本示意)
```
+-------------------+
|   主窗口 (UI)     |
| - 时间设置        |
| - 模式选择        |
| - 外观设置        |
+-------------------+
          |
          v
+-------------------+
|  控制器 (Manager) |
| - PomodoroTimerManager |
| - 协调UI与核心逻辑|
+-------------------+
          |
          +-------------------+
          |                   |
          v                   v
+-------------------+   +-------------------+
| 置顶窗口 (UI)     |   | 设置管理 (Service)|
| - OverlayWidget   |   | - QSettings       |
| - 显示时间        |   | - 保存/加载配置   |
| - 透明度/尺寸/颜色|   +-------------------+
+-------------------+
| - QTimer (核心逻辑)|
+-------------------+
```

#### 3.3 核心组件详解

*   **主窗口 (Main Window)**:
    *   **类型**: `QWidget` 或 `QMainWindow`
    *   **职责**: 提供用户交互界面，用于设置计时参数、选择模式、调整外观，并触发计时器的启动。
    *   **依赖**: `PomodoroTimerManager`

*   **置顶计时器窗口 (Overlay Widget)**:
    *   **类型**: `QWidget`
    *   **职责**: 显示倒计时数字，始终保持在最前端，响应拖动操作，应用外观设置。
    *   **实现要点**:
        *   `setWindowFlags(Qt::WindowStaysOnTopHint | Qt::FramelessWindowHint)`
        *   `setAttribute(Qt::WA_TranslucentBackground)`
        *   使用 `paintEvent` 或 `QLabel` + `setStyleSheet` 实现自定义渲染。
    *   **依赖**: `PomodoroTimerManager` (接收计时数据和外观指令)

*   **控制器 (PomodoroTimerManager)**:
    *   **类型**: 自定义类
    *   **职责**: 模块的核心逻辑中枢。
        *   解析用户设置，决定是单次还是循环计时。
        *   管理 `QTimer` 实例，执行倒计时逻辑。
        *   在循环模式下，管理“工作”与“休息”阶段的切换及循环计数。
        *   接收来自UI的指令（开始、暂停、停止），并向UI（特别是`OverlayWidget`）发送更新指令（更新时间、更新状态、触发提示）。
        *   调用`SettingsManager`保存和读取配置。
    *   **依赖**: `QTimer`, `SettingsManager`

*   **设置管理 (SettingsManager)**:
    *   **类型**: 单例类或静态方法类
    *   **职责**: 使用 `QSettings` 读写用户配置，如默认时长、透明度、背景色等。
    *   **依赖**: `QSettings`

### 4. 模块二：桌面待办事项 (Desktop To-Do List)

#### 4.1 模块概述
实现一个可固定在桌面的待办列表，支持任务管理、状态跟踪和日志记录。

#### 4.2 模块架构图 (文本示意)
```
+-------------------+
|   主窗口 (UI)     |
| - 初始化入口      |
| - 添加任务        |
| - 导入/导出按钮   |
+-------------------+
          |
          v
+-------------------+
|  控制器 (Manager) |
| - ToDoController  |
| - 协调UI与核心逻辑|
+-------------------+
          |
          +------------------+------------------+
          |                  |                  |
          v                  v                  v
+-------------------+  +-------------------+  +-------------------+
| 桌面待办窗口 (UI) |  | 任务管理 (Logic)   |  | 日志管理 (Service)|
| - DesktopToDoWidget|  | - TaskManager     |  | - LogManager      |
| - 显示任务列表    |  | - 任务CRUD/状态   |  | - 记录/导出/导入  |
| - 任务项Widget    |  +-------------------+  +-------------------+
+-------------------+                                    ^
| - 系统托盘控制器   |                                    |
| - SystemTrayCtrl  | ------------------------------------+
+-------------------+
```

#### 4.3 核心组件详解

*   **主窗口 (Main Window)**:
    *   **类型**: `QWidget` (作为工具箱主框架的一部分)
    *   **职责**: 提供待办列表的初始化界面，包含添加任务的输入框和按钮，以及日志导入/导出的入口。
    *   **依赖**: `ToDoController`

*   **桌面待办窗口 (Desktop To-Do Widget)**:
    *   **类型**: `QWidget`
    *   **职责**: 以独立窗口形式显示待办列表，支持置顶/固定模式和拖动。
    *   **实现要点**:
        *   根据模式设置 `setWindowFlags` (`Qt::WindowStaysOnTopHint` / `Qt::Tool | Qt::FramelessWindowHint`)。
        *   使用 `QVBoxLayout` 动态添加/移除 `TaskItemWidget`。
        *   实现 `mousePressEvent` 和 `mouseMoveEvent`。
    *   **依赖**: `TaskManager` (获取任务数据)

*   **任务项UI (Task Item Widget)**:
    *   **类型**: `QWidget`
    *   **职责**: 表示单个任务，包含复选框、标签和滑动条。
    *   **实现要点**: 布局 (`QHBoxLayout`)，信号 (`stateChanged`, `valueChanged`)。

*   **系统托盘控制器 (SystemTray Controller)**:
    *   **类型**: 自定义类
    *   **职责**: 管理 `QSystemTrayIcon`，创建菜单，响应菜单点击事件，并调用 `DesktopToDoWidget` 的方法来控制其显示/隐藏和模式。
    *   **依赖**: `QSystemTrayIcon`, `DesktopToDoWidget`

*   **控制器 (ToDoController)**:
    *   **类型**: 自定义类
    *   **职责**: 模块的核心逻辑中枢。
        *   连接 `Main Window`、`DesktopToDoWidget`、`TaskManager` 和 `LogManager`。
        *   响应UI事件（如添加任务、任务状态变更），并委托给 `TaskManager` 和 `LogManager` 处理。
        *   协调系统托盘命令与 `DesktopToDoWidget` 的状态。
    *   **依赖**: `TaskManager`, `LogManager`, `DesktopToDoWidget`

*   **任务管理 (TaskManager)**:
    *   **类型**: 自定义类
    *   **职责**: 管理任务的核心数据和逻辑。
        *   维护 `QList<TaskData>`。
        *   实现任务的增删改查（CRUD）。
        *   更新任务的完成状态和评分。
        *   发出信号通知UI更新。
    *   **依赖**: `TaskData` 结构体

*   **日志管理 (LogManager)**:
    *   **类型**: 自定义类
    *   **职责**: 负责日志的记录、存储和导出/导入。
        *   维护 `QList<LogEntry>`。
        *   提供 `logCreate()`, `logComplete()`, `logRate()` 等方法供 `TaskManager` 调用。
        *   提供 `exportLogs()` 和 `importLogs()` 方法处理文件I/O。
    *   **依赖**: `LogEntry` 结构体, `QJsonDocument` / `QTextStream` (用于文件操作)

*   **数据结构 (Data Structures)**:
    *   **TaskData**: `struct { int id; QString description; bool isCompleted; int completionScore; QDateTime creationTime; QDateTime completionTime; }`
    *   **LogEntry**: `struct { QDateTime timestamp; QString action; QString taskDescription; int score; }`

---
这份文档集合包含了开发说明、软件需求说明书和架构设计文档的Markdown格式版本，可以直接用于项目管理和文档存档。